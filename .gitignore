# secrets & local config — NEVER commit
.env
config.json

# runtime data
data/
*.db
*.db-shm
*.db-wal
*.log

# python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# node
frontend/node_modules/

# packaging build artifacts (portable bundle) — the sources are committed, not the output
packaging/build/
packaging/dist/
packaging/*.spec

# OS
.DS_Store
Thumbs.db

# controller scratch / local dev tooling — NEVER commit
.superpowers/
.claude/
.playwright-mcp/
