// Placeholder — Task 11 lifts the real dataset components (gallery, upload,
// captioning, face scoring) from SRC and replaces this file.
export default function DatasetsPage() {
  return (
    <div className="rounded-xl border border-border bg-surface p-8 text-center">
      <h1 className="text-lg font-semibold text-content">Datasets</h1>
      <p className="mt-2 text-sm text-content-muted">
        The dataset workspace lands in a follow-up task.
      </p>
    </div>
  )
}
