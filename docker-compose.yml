services:
  studio:
    build: .
    ports: ["5000:5000"]
    volumes:
      - ./data-docker:/data
      - ./.env:/app/.env
    environment:
      - LDS_CONFIG=/data/config.json
      - LDS_HOST=0.0.0.0
